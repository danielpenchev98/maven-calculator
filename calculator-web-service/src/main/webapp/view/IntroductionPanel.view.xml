<mvc:View
        controllerName="com.calculator.web.ui.controller.IntroductionPanel"
        xmlns="sap.m"
        xmlns:mvc="sap.ui.core.mvc">
    <Panel
            class="sapUiResponsiveMargin"
            width="auto"
            headerText="{i18n>introHeaderText}">
        <content>
            <Text text="{i18n>introductionContent}"/>
        </content>
    </Panel>
</mvc:View>